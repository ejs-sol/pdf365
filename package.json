{
  "name": "pdf365-docs",
  "version": "0.0.1",
  "scripts": {
    "build": "eleventy && find _site -type d",
    "start": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
